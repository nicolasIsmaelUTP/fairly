"""fairly: Bias evaluation toolkit for Vision-Language Models (VLMs).

Usage:
    import fairly

    # Run the dashboard
    # $ fairly ui

    # Programmatic evaluation
    # results = fairly.evaluate(model=my_model, dataset=my_dataset)
"""

__version__ = "0.1.0"
